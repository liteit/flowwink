# Pre-baseline migrations (archived 2026-06-08)

These 453 migrations were squashed into `supabase/migrations/00000000000000_baseline.sql`.
They are kept for reference only — **do not run them**. The Supabase CLI ignores
this folder (it only reads top-level `supabase/migrations/*.sql`).
