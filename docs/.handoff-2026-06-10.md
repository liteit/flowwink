# Handoff — 2026-06-10 (cold pickup for the next agent)

Read this + `docs/parity/verification-loop.md` first, then continue.

## Where we are
- **Odoo-parity program is now on `main`**: `docs/parity/` (scorecards per module,
  dual-surface law, epics, roadmap) + `scripts/parity-report.ts`
  (`bun run scripts/parity-report.ts` → current matrix; hr 74%, accounting 45%,
  timesheets 44%, …). **Goal: every module ≥ 80% Odoo depth.**
- **Auth surface is OPEN**: 44 admin-gated RPCs were dead for agents
  (`has_role(auth.uid(),'admin')` vs service key). Opened to
  `(auth.role()='service_role' OR has_role(...))` on all 4 instances. Human
  sessions keep the same admin requirement.
- Tonight's fixes (all fleet-applied + deployed): accounting reports, expense
  book/pay, payroll posting, AR posting (invoices→ledger), quote-to-cash,
  pricelist, place_order, chat-completion guard, ecommerce skillSeed hole, sticky
  circuit breaker, create_payroll_run drift, fixed-asset posting drift.
  Full detail: `/tmp/fw-overnight-log.md`; module pass: `docs/.module-test-run-2026-06-10.md`.

## NEXT WORK — arg-contract drift (well-scoped, start here)
These RPC skills were never run, so their **skill schema drifted from the function
signature**. Fix = align the skill seed in `src/lib/modules/<module>-module.ts`
(or add a converting `mcp_*` wrapper), then `npm run skills:json` +
`npm run sync:skills -- --apply` per instance. NOT data bugs.

- `close_accounting_period` — skill passes `period_end`; fn is
  `(p_year int, p_month int, p_notes text)`. → add `mcp_close_accounting_period`
  wrapper that derives year+month from a date, or change the skill schema.
- `adjust_quant` — skill `{product_id, quantity, reason}`; fn
  `(p_product_id, p_location_id, p_qty_delta, p_lot_id, p_reason)`. Skill is
  missing `location_id`; `quantity` vs `qty_delta`.
- `create_bom` — skill `{action, components, product_name}`; fn
  `(p_product_id, p_lines, ...)`. `product_name` vs `product_id`, `components`
  vs `lines`.

Verify each with the loop in `docs/parity/verification-loop.md` BEFORE marking the
matching capability `done`.

Also queued: `draft_candidate_outreach` needs a real handler (it's wired to raw
edge:chat-completion but its contract is `{application_id, tone, outreach_type}` —
crash already guarded, but it can't draft); `documents` create needs `file_name`
surfaced; FlowPilot↔FlowWink terminology audit in customer-facing templates.

## Operational facts (no secrets here — see the referenced files)
- **Local loop** (the thing that finds these bugs): `orb start` → `supabase start`
  → `supabase functions serve --no-verify-jwt --env-file supabase/functions/.env`.
  Local stack was left running. Test via
  `POST http://127.0.0.1:54321/functions/v1/agent-execute` with
  `{skill_name, arguments, agent_type:"flowpilot"}` (Bearer `local-sim`).
- **Fleet refs + sync runbook**: `docs/operators/provisioning-and-updates.md`,
  `scripts/fleet.json`. Edge fns do NOT auto-deploy.
- **Deploy creds**: per-instance Supabase tokens are in `.env.local` (commented,
  `# <site> <ref> <token>`); the shared DB password the owner provided is used for
  `psql`/migrations and `sync:skills`. The owner rotates these — don't persist
  them anywhere.
- **Demo is the safe test instance** (resets hourly, no email since composio/resend
  are off). A demo test API key for `/rest/execute` is in `/tmp/fw-demo-token.txt`.

## The rule
If you didn't RUN it locally and see the right DB effect, it isn't done. The
scorecard measures verified reality. (This is why payroll was "done" on paper but
crashed on the first real call.)
